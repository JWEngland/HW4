<h1>Places</h1>

<ul>
  <% @places.each do |place| %>
    <li>
      <%= link_to place.name, place_path(place) %>
    </li>
  <% end %>
</ul>

<p>
  <% if current_user %>
    <%= link_to 'New place', new_place_path %>
  <% else %>
    Please log in to create a new place.
  <% end %>
</p>
